# Secrets and local config — never commit
# profile.json (repo root) is the live one. The other two are paths this lab used
# earlier; kept because this repo is public and a stale token must never land in it.
profile.json
secret.pat
producer/profile.json
*.p8
*.pem
*.key
.env

# Python
.venv/
venv/
__pycache__/
*.pyc
*.egg-info/
.pytest_cache/
.ruff_cache/

# Build artifacts from `snow streamlit deploy` — it stages a copy of the app
dashboard/output/

# Facilitator-only materials — kept local, not published to the attendee repo
outline.md
script.md
rehearsal.md
PRESENTER.md

# OS / editor cruft
.DS_Store
.idea/
.vscode/
