# Producer dependencies.
#
# Versions are pinned because a 90-minute lab cannot absorb a surprise upstream
# release. If you are running this outside the lab, feel free to unpin.

# Snowpipe Streaming SDK -- the high-performance architecture, and the only one
# that supports Iceberg v3 targets. Used for the telemetry feed.
snowpipe-streaming==1.7.1

# Used by the CDC journal path and the direct fallback path. In the journal path
# it drives the MERGE that the connector's merge processor issues over its own
# Snowflake connection (see merge_loop()). In the direct path it writes settled
# DML straight to the destination table.
snowflake-connector-python==4.7.2
